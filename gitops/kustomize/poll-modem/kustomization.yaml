apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: poll-modem

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
  - dashboard-configmap.yaml
  - downstream-signal-dashboard-configmap.yaml
  - ingress.yaml

# Create the modem credentials secret manually, not in git:
# kubectl create secret generic modem-credentials \
#   --namespace poll-modem \
#   --from-literal=username=admin \
#   --from-literal=password='<modem-password>'
